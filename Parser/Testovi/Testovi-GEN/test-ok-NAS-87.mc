//OPIS: unsigned func, num_exp as return value
//RETURN: 0
Fun unsigned Func << unsigned K >>  
(
    int H,B;
    int C,D,E,F;
    unsigned G = 0u; //mora imati dodeljenu vrednost zbog individualnog
		H = 0; 
		B = 2; 
		C = 2; 
		D = C.inkr; 
		E = B.inkr; 
		E = 2; 
		//F = K; razliciti tipovi nemoguca je dodela

		return (G + K);

)

Fun int Main << >> 
(
	return 0;
)

